//add lineheight 
// add themecontext

const breakpoints = ['768px', '992px', '1200px'];

const colors = {
  black: '#333333',
  lightGrey: '#797979',
  grey: '#666666'
}

const fonts = {
  default: 'Helvetica Neue'
}
const fontSizes = {
  regular: '18px'
}

export default {
  breakpoints,
  colors,
  fontSizes,
  fonts
}